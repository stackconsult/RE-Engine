# Build Plan — Next Steps (Execution Order)

1) Run bootstrap scripts
2) Verify smoke test passes
3) Implement remaining store features (locking, CSV robust parsing)
4) Implement real channel adapters (email/WA/TG) with env-based config
5) Implement MCP servers (reengine-core first)
6) Implement Playwright self-healing + artifact pipeline
7) Wire OpenClaw cron + approvals UI integration
8) Add CI scripts and full test coverage
