services:
  engine:
    build:
      context: .
      dockerfile: engine/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
    env_file:
      - .env
    restart: always

  # Mock Database/Services for local production testing can be added here
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: reengine
  #     POSTGRES_PASSWORD: password
